Vec2 := struct {
    x: int,
    y: int
};

f := proc(i: Vec2) void {
    i'x = 4;
    print("i is", i'x);
};

main := proc () void {
    x := new Vec2 { x: 2, y: 3 };
    i := 3;
    f(x);
    print(x'x, i);
};
